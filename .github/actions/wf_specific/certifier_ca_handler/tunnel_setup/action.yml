name: "tunnel_setup"
description: "SSH port-forward tunnel to NCM/CMP endpoints on the remote AWS instance"
inputs:
  SSH_KEY:
    description: "SSH access key"
    required: true
  SSH_KNOWN_HOSTS:
    description: "SSH known hosts"
    required: true
  SSH_USER:
    description: "SSH user"
    required: true
  SSH_HOST:
    description: "SSH host (DNS name or IP); overridden by EC2 public IP when DNS is stale"
    required: true
  SSH_PORT:
    description: "SSH port"
    required: true
  NAME_SPACE:
    description: "namespace"
    required: true
    default: "acme"
  NCM_API_HOST:
    description: "NCM API host"
    required: true
  NCM_API_USER:
    description: "NCM API user"
    required: true
  NCM_API_PASSWORD:
    description: "NCM API password"
    required: true
  AWS_ACCESS_KEY_ID:
    description: "Optional AWS key to resolve live EC2 public IP when DNS is stale"
    required: false
    default: ""
  AWS_SECRET_ACCESS_KEY:
    description: "Optional AWS secret"
    required: false
    default: ""
  AWS_REGION:
    description: "Optional AWS region"
    required: false
    default: ""
  AWS_INSTANCE_ID:
    description: "Optional EC2 instance id used for public IP lookup"
    required: false
    default: ""
runs:
  using: "composite"
  steps:
  - name: "Prepare ssh environment on ramdisk"
    run: |
      set -euo pipefail
      sudo mkdir -p /tmp/rd
      sudo mount -t tmpfs -o size=5M none /tmp/rd || true
      printf '%s\n' "${SSH_KEY}" | sudo tee /tmp/rd/ak.tmp >/dev/null
      sudo chmod 600 /tmp/rd/ak.tmp
      printf '%s\n' "${KNOWN_HOSTS}" | sudo tee /tmp/rd/known_hosts >/dev/null
      sudo chown "$(id -u):$(id -g)" /tmp/rd/ak.tmp /tmp/rd/known_hosts 2>/dev/null || true
    env:
      SSH_KEY: ${{ inputs.SSH_KEY }}
      KNOWN_HOSTS: ${{ inputs.SSH_KNOWN_HOSTS }}
    shell: bash

  - name: "Resolve SSH connect host (DNS vs EC2 public IP)"
    id: resolve_host
    run: |
      set -euo pipefail
      CONNECT_HOST="${SSH_HOST}"
      DNS_IP="$(getent ahostsv4 "${SSH_HOST}" | awk '{print $1; exit}' || true)"
      echo "SSH_HOST DNS ${SSH_HOST} -> ${DNS_IP:-unresolved}"

      if [[ -n "${AWS_ACCESS_KEY_ID}" && -n "${AWS_SECRET_ACCESS_KEY}" && -n "${AWS_REGION}" && -n "${AWS_INSTANCE_ID}" ]]; then
        if ! command -v aws >/dev/null 2>&1; then
          pip3 install --user awscli >/dev/null
          export PATH="${HOME}/.local/bin:${PATH}"
        fi
        aws configure set aws_access_key_id "${AWS_ACCESS_KEY_ID}"
        aws configure set aws_secret_access_key "${AWS_SECRET_ACCESS_KEY}"
        aws configure set default.region "${AWS_REGION}"
        export AWS_MAX_ATTEMPTS=1
        PUBLIC_IP=""
        for attempt in 1 2 3 4 5; do
          # Cap connect wait; without this, runner→AWS timeouts burn ~3m per attempt.
          if PUBLIC_IP="$(aws ec2 describe-instances \
            --region "${AWS_REGION}" \
            --instance-ids "${AWS_INSTANCE_ID}" \
            --cli-connect-timeout 10 \
            --cli-read-timeout 30 \
            --query 'Reservations[0].Instances[0].PublicIpAddress' \
            --output text 2>/tmp/aws_ec2_err.txt)"; then
            break
          fi
          echo "WARNING: aws ec2 describe-instances attempt ${attempt}/5 failed:"
          cat /tmp/aws_ec2_err.txt >&2 || true
          PUBLIC_IP=""
          sleep $((attempt * 2))
        done

        if [[ -n "${PUBLIC_IP}" && "${PUBLIC_IP}" != "None" ]]; then
          echo "EC2 public IP for ${AWS_INSTANCE_ID}: ${PUBLIC_IP}"
          if [[ -z "${DNS_IP}" || "${DNS_IP}" != "${PUBLIC_IP}" ]]; then
            echo "WARNING: DNS ${DNS_IP:-unresolved} != EC2 public IP ${PUBLIC_IP}; using EC2 IP for tunnel"
            CONNECT_HOST="${PUBLIC_IP}"
          else
            echo "DNS matches EC2 public IP; using ${SSH_HOST}"
          fi
        else
          echo "WARNING: EC2 public IP lookup failed or empty; falling back to SSH_HOST=${SSH_HOST}"
        fi
      else
        echo "AWS inputs not fully set; using SSH_HOST=${SSH_HOST}"
      fi

      echo "connect_host=${CONNECT_HOST}" >> "${GITHUB_OUTPUT}"
      echo "Tunnel SSH target: ${CONNECT_HOST}"
    env:
      SSH_HOST: ${{ inputs.SSH_HOST }}
      AWS_ACCESS_KEY_ID: ${{ inputs.AWS_ACCESS_KEY_ID }}
      AWS_SECRET_ACCESS_KEY: ${{ inputs.AWS_SECRET_ACCESS_KEY }}
      AWS_REGION: ${{ inputs.AWS_REGION }}
      AWS_INSTANCE_ID: ${{ inputs.AWS_INSTANCE_ID }}
    shell: bash

  - name: "Setup ssh forwarder"
    run: |
      docker run -d --rm --network "${NAME_SPACE}" --name=forwarder \
        -e "MAPPINGS=8084:127.0.0.1:8084; 8080:127.0.0.1:8080" \
        -e "SSH_HOST=${SSH_HOST}" \
        -e "SSH_PORT=${SSH_PORT}" \
        -e "SSH_USER=${SSH_USER}" \
        -p 8080:8080 -p 8084:8084 \
        -v "/tmp/rd/ak.tmp:/ssh_key:ro" \
        davidlor/ssh-port-forward-client:dev
    env:
      SSH_USER: ${{ inputs.SSH_USER }}
      SSH_HOST: ${{ steps.resolve_host.outputs.connect_host }}
      SSH_PORT: ${{ inputs.SSH_PORT }}
      NAME_SPACE: ${{ inputs.NAME_SPACE }}
      NCM_API_HOST: ${{ inputs.NCM_API_HOST }}
    shell: bash

  - name: "Wait for NCM API via ssh tunnel"
    run: |
      set -euo pipefail
      # NCM root often returns 404; any HTTP response means the tunnel is up.
      # Do not use curl --fail (exit 22 on 4xx).
      for i in $(seq 1 30); do
        code="$(docker run -i --rm --network "${NAME_SPACE}" curlimages/curl \
          --silent --show-error --insecure \
          --output /dev/null --write-out '%{http_code}' \
          "https://${NCM_API_USER}:${NCM_API_PASSWORD}@forwarder.acme:8084" \
          || true)"
        echo "attempt ${i}/30 http_code=${code}"
        if [[ "${code}" =~ ^[1-5][0-9]{2}$ ]]; then
          echo "tunnel ready after ${i} attempt(s)"
          exit 0
        fi
        sleep 2
      done
      echo "ERROR: NCM API tunnel not ready" >&2
      docker logs forwarder 2>&1 || true
      exit 1
    env:
      NCM_API_HOST: ${{ inputs.NCM_API_HOST }}
      NAME_SPACE: ${{ inputs.NAME_SPACE }}
      NCM_API_USER: ${{ inputs.NCM_API_USER }}
      NCM_API_PASSWORD: ${{ inputs.NCM_API_PASSWORD }}
    shell: bash
